import { css } from '@emotion/react';

const profileAvatarStyle = css`
    background: hsla(0, 0%, 100%, 0.2);
    color: hsla(0, 0%, 100%, 0.9);

    .MuiChip-avatar {
        color: inherit;
    }
`;

export { profileAvatarStyle };
