import { SearchChipType } from '@chlee526/ui-kit-react';

interface BoardFunctionProps {
    updateSearchParameter: ({ searchType, searchKeyword }: SearchChipType) => void;
}

export type { BoardFunctionProps };
